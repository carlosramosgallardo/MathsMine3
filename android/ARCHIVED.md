# TWA shell — archived

Replaced by the native client in `apps/android-native/` (same `applicationId`: `xyz.mathsmine3.app`).

Keep this folder only as historical reference / emergency web-wrapper builds.
Prefer: `npm run android:native:build`
